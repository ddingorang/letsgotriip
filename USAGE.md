# Triip 사용법 가이드

여행 계획 · 커뮤니티 · 동행 · AI 어시스턴트를 한 곳에서 다루는 여행 앱입니다.
설치/인프라는 [SETUP.md](SETUP.md), 이 문서는 **실행 후 앱을 어떻게 쓰는지**를 다룹니다.

---

## 1. 빠른 시작 (Docker)

루트에 `.env`를 만들고([.env.example](.env.example) 복사) 값을 채운 뒤:

```bash
docker compose up -d --build
```

전체 스택(MySQL·MongoDB·Redis·Redis-Vector·RabbitMQ + 백엔드 + 프론트엔드)이 한 번에 뜹니다.

| 대상 | 주소 |
|------|------|
| **프론트엔드(웹앱)** | http://localhost:5173 |
| 백엔드 API | http://localhost:9090 |
| RabbitMQ 관리 UI | http://localhost:15672 (guest / guest) |

> `.env`의 `OPENAI_API_KEY`(챗봇·STT), `TOUR_API_KEY`(관광지), `VITE_KAKAO_MAP_KEY`(지도)가 있어야 해당 기능이 완전히 동작합니다. 비워도 앱은 뜨며 그 기능만 비활성화됩니다.

### 데모 계정 (시드)

| 역할 | 이메일 | 비밀번호 |
|------|--------|----------|
| 일반 사용자 | `demo@triip.com` | `demo1234` |
| 관리자 | `admin@triip.com` | `admin1234` |

로그인 폼은 기본적으로 데모 계정이 미리 채워집니다(`.env`의 `VITE_DEMO_*`로 변경/비활성 가능). 직접 **회원가입**하거나 **Google 로그인**도 가능합니다.

---

## 2. 화면별 사용법

### 🏠 홈 (`/home`)
- 내 위치 기반 추천 관광지(권한 허용 시), 위치 거부 시 제주 인기 폴백
- 지금 뜨는 여행지·커뮤니티 인기글·여행 뉴스(데모 데이터)

### 🧭 탐색 (`/explore`)
- 카테고리 필터 + 무한 스크롤로 관광지 목록 탐색
- **지도의 플래그(마커)를 누르면** 해당 위치로 지도가 이동하고 아래 목록에서 선택됩니다
- 거리순/인기순 정렬, 카드 클릭 시 상세(`/place/:id`)

### 📍 관광지 상세 (`/place/:id`)
- 별점·리뷰 작성/수정/삭제(본인 글만), 실제 리뷰 목록 표시
- **찜(북마크)** — 마이페이지의 찜 목록에 반영
- "내 계획에 추가"로 여행 일정에 담기

### 🗓️ 여행 계획 (`/plan`)
- 일자별 일정 만들기, 장소 추가/순서 변경
- **계획을 지도에 표시** — 일정의 장소들이 지도 마커로
- **평가 받기** / **챗봇으로 이동해 수정** (AI 어시스턴트 연계)
- 공유 링크 생성(`/plan/shared/:token`, 비로그인도 열람), 예산 비교

### 💬 커뮤니티 (`/community`)
- 카테고리 탭(후기·질문·꿀팁·맛집·동행) — **서버 필터**로 정확히 분류
- 글 작성(`/community/write`, 사진 첨부 업로드), 좋아요·댓글(페이지네이션)·북마크
- 작성자 **팔로우**, 댓글 좋아요

### 🔥 핫플레이스 (`/hotplace/register`, `/hotplace/:id`)
- 카카오 지도에서 위치 지정 + 사진 첨부로 등록
- 등록 시 **관리자 승인 대기(PENDING)** 상태 → 승인 후 공개 목록/지도에 노출
  (관리자 모더레이션 기능을 실제로 사용하려는 설계. 바로 보이게 하려면 admin 계정으로 승인)

### 🤝 동행 (`/companion/:id`)
- 동행 모집글 + **모집 조건·정원** 표시, 일정이 있으면 **지도에 표시**
- 신청 → 방장이 **수락/거절**(`/companion/:id/applicants`), 신청 취소 가능
- 승인되면 **동행 채팅방** 입장 버튼 노출, 정원 초과/축소 방지

### 🗨️ 채팅 (`/chat`, `/chat/:id`)
- 동행 그룹 실시간 채팅(STOMP/WebSocket, 메시지 영속)
- 방 설정(방장): 제목·소개·**방 이미지 변경**, 참여자 관리(강퇴/초대/방장 위임), 음소거

### 🤖 AI 어시스턴트 (`/assistant`)
- **선택형 폼**으로 여행 계획 세우기(자유 질의 + 선택 입력 병행)
- **음성·문서·텍스트** 입력 지원 — 음성은 STT 변환, 문서(txt/pdf)는 RAG 색인
- 기존/신규 계획 **평가 받기**, 추천을 실제 계획으로 저장
- 응답이 일부만 구성되면 임의 저장하지 않고 다시 시도 안내

### 📄 문서 (`/documents`)
- txt/pdf 업로드 → RAG 색인 후 챗봇이 참고. 텍스트가 없는 문서는 '내용 없음'으로 정직 표기

### 👤 마이페이지 (`/mypage`)
- 내 계획 / **앨범**(생성·사진·**공유 링크**) / **뱃지·레벨**(게임화 실데이터)
- 내 **리뷰** 목록, **찜**·**좋아요한 글**, **팔로잉** 목록
- 프로필/이미지 변경(`/mypage/edit`), 챌린지(`/mypage/challenge`)
- 알림(`/notifications`) — 클릭 시 해당 위치/대상으로 이동
- **관리자 계정**이면 '운영 관리' 진입(`/admin`)

### 🛡️ 관리자 (`/admin`) — admin 계정 전용
- 핫플레이스 **승인 대기 목록** 확인 → **승인/거절**
- 공지 작성·수정·삭제
  (비관리자는 라우터 가드 + 서버 권한(403)으로 차단)

### 🖼️ 앨범 공유 (`/album/shared/:token`)
- 마이페이지 앨범에서 공유 버튼 → 토큰 링크 복사 → 누구나(비로그인) 읽기 전용 열람

---

## 3. 참고 / 한계 (데모)

- **결제·예약**(`/payment`, `/confirmation`)은 데모 화면입니다 — 실제 PG 연동/결제는 일어나지 않습니다.
- **STT**는 현재 목업(MockSTTManager)으로 동작합니다 — 실제 음성 인식을 쓰려면 Whisper 연동(`OPENAI_API_KEY`) 전환이 필요합니다.
- 뉴스·전기차 충전소·그룹 할인 등 일부 데이터는 외부 연동 없이 **데모 데이터**입니다.
- 시드 데이터(제주 중심 핫플/게시글/동행)는 데모 계정용으로 미리 들어 있습니다.

---

## 4. 개발/검증 메모

- 백엔드 컴파일: `cd BE && ./gradlew compileJava`
- 프론트 빌드: `cd frontend && npm run build`
- 경로 규칙: FE는 `/api` 프록시 사용. 백엔드는 `community`·`companion`·`auth`·`users`는 `/api` 미사용, 그 외(`favorites`·`follows`·`groups`·`attractions`·`reviews`·`chat`·`search`·`notices` 등)는 `/api` 유지.
- 자세한 설치/인프라: [SETUP.md](SETUP.md), 아키텍처: [docs/system-design.md](docs/system-design.md)
